{
  "name": "weddingmarch-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 8092",
    "start": "astro dev --host 0.0.0.0 --port 8092",
    "build": "astro build",
    "build:sites": "astro build && node scripts/prepare-sites-build.mjs",
    "verify:crm-content": "node scripts/verify-crm-content.mjs",
    "preview": "astro preview --host 0.0.0.0 --port 8092",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.15.0",
    "@astrojs/tailwind": "^5.1.0",
    "tailwindcss": "^3.4.0"
  }
}
