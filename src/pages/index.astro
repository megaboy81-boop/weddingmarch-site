---
import Layout from '../layouts/Layout.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title = '춘천웨딩드레스 웨딩마치 | 2회 피팅 · 드레스 120벌 · 미래웨딩홀 입점';
const description = '강원도 춘천 웨딩드레스 전문샵 웨딩마치. 드레스 대여부터 헤어·메이크업·도우미까지 원스톱 웨딩 서비스. 미래웨딩홀 1층 입점, 2회 피팅 시스템.';

const storyPanels = [
  {
    bg: `${base}/images/bg-new/h1.jpg`,
    eyebrow: 'Chuncheon Bridal Salon · Est. 2017',
    title: 'Wedding\nMarch',
    titleItalic: true,
    desc: '촬영보다 본식이 더 예뻤어요',
    isHero: true,
  },
  {
    bg: `${base}/images/bg-new/h2.jpg`,
    eyebrow: 'Our Collection',
    title: '드레스 120벌',
    desc: '국내외 최신 트렌드 웨딩드레스 120벌 보유. 에이라인, 볼가운, 슬림핏, 레이스까지 모든 스타일.',
    stat: '120+',
    statLabel: '드레스',
  },
  {
    bg: `${base}/images/bg-new/h3.jpg`,
    eyebrow: 'Signature System',
    title: '2회 피팅',
    desc: '3개월 전 1차 피팅(3벌) + 1개월 전 2차 피팅(3벌). 타샵과 차별화된 전문 피팅 시스템.',
    stat: '2회',
    statLabel: '피팅',
  },
  {
    bg: `${base}/images/bg-new/h4.jpg`,
    eyebrow: 'All-in-One Wedding',
    title: '원스톱 웨딩',
    desc: '드레스 대여 + 헤어·메이크업 + 도우미까지. 미래웨딩홀 1층 입점으로 이동 없이 모든 준비를.',
    stat: '7년+',
    statLabel: '춘천 No.1',
  },
];

const galleryItems = [
  { src: `${base}/images/instagram/630.webp`, label: '매장 인테리어' },
  { src: `${base}/images/instagram/790.webp`, label: '드레스 컬렉션' },
  { src: `${base}/images/instagram/815.webp`, label: '매장 전경' },
  { src: `${base}/images/instagram/860.webp`, label: '드레스 진열' },
  { src: `${base}/images/instagram/920.webp`, label: '매장 내부' },
  { src: `${base}/images/instagram/1080.webp`, label: '드레스 컬렉션' },
  { src: `${base}/images/instagram/1100.webp`, label: '매장 인테리어' },
  { src: `${base}/images/instagram/1110.webp`, label: '드레스 진열' },
  { src: `${base}/images/instagram/1140.webp`, label: '매장 전경' },
  { src: `${base}/images/instagram/1160.webp`, label: '드레스 컬렉션' },
  { src: `${base}/images/instagram/1170.webp`, label: '매장 내부' },
  { src: `${base}/images/instagram/1180.webp`, label: '드레스 진열' },
];

const reviews = [
  { text: '촬영보다 본식이 더 예뻤어요. 드레스 퀄리티가 정말 달랐고, 2회 피팅 덕분에 완벽한 드레스를 찾을 수 있었습니다.', name: '춘천 신부', date: '2025년 10월', img: `${base}/images/instagram/01.webp` },
  { text: '미래웨딩홀 바로 아래에 있어서 결혼식 당일 너무 편리했어요. 헤어메이크업부터 도우미까지 모두 한번에 해결됐습니다.', name: '강원 신부', date: '2025년 5월', img: `${base}/images/instagram/04.webp` },
  { text: '드레스가 너무 예뻐서 피팅하면서 눈물이 났어요. 직원분들이 너무 친절하게 도와주셨고 최고의 날이었습니다.', name: '홍천 신부', date: '2025년 11월', img: `${base}/images/instagram/07.webp` },
];

const stats = [
  { num: 120, suffix: '+', label: '드레스 보유' },
  { num: 7, suffix: '년+', label: '춘천 No.1' },
  { num: 2, suffix: '회', label: '피팅 시스템' },
  { num: 98, suffix: '%', label: '신부 만족도' },
];
---
<Layout title={title} description={description}>

<!-- ══════ 골드 파티클 캔버스 ════════════════════════════════════════════ -->
<canvas id="particle-canvas" style="position:fixed;inset:0;z-index:1;pointer-events:none;opacity:0.6;"></canvas>

<!-- ══════ VIDEO SCROLL — 섹션 1 (frames 001~064) ════════════════════════ -->
<section id="vs1" style="height:350vh;position:relative;">
  <div style="position:sticky;top:0;height:100dvh;overflow:hidden;background:#070305;">
    <canvas id="vc1" style="position:absolute;inset:0;width:100%;height:100%;z-index:1;transition:opacity 0.6s ease;"></canvas>
    <div style="position:absolute;inset:0;z-index:2;background:linear-gradient(to bottom,rgba(7,3,5,0.3) 0%,rgba(7,3,5,0.05) 50%,rgba(7,3,5,0.75) 100%);pointer-events:none;"></div>
    <div style="position:absolute;inset:0;z-index:2;background:radial-gradient(ellipse at center,transparent 50%,rgba(7,3,5,0.4) 100%);pointer-events:none;"></div>
    <!-- 텍스트 -->
    <div style="position:absolute;inset:0;z-index:3;display:flex;flex-direction:column;justify-content:flex-end;padding:0 8vw 12vh;pointer-events:none;">
      <div style="max-width:750px;">
        <div style="display:flex;align-items:center;gap:16px;margin-bottom:28px;overflow:hidden;">
          <span id="eyebrow-line" style="display:block;height:1px;width:0;background:linear-gradient(90deg,transparent,rgba(212,175,117,0.6));transition:width 1.2s ease 0.5s;"></span>
          <span id="eyebrow-text" style="font-size:10px;letter-spacing:0.55em;text-transform:uppercase;color:rgba(255,255,255,0.45);opacity:0;transition:opacity 0.8s ease 0.9s;">Chuncheon Bridal Salon · Est. 2017</span>
        </div>
        <h1 class="font-display" style="font-size:clamp(68px,12vw,148px);font-weight:300;color:white;line-height:0.88;letter-spacing:0.02em;margin-bottom:30px;">
          <div style="overflow:hidden;display:block;"><span class="title-slide-up" style="display:block;animation-delay:0.4s;">Wedding</span></div>
          <div style="overflow:hidden;display:block;"><span class="gold-gradient title-slide-up" style="display:block;font-style:italic;animation-delay:0.65s;">March</span></div>
        </h1>
        <p id="hero-sub" style="font-size:clamp(14px,1.8vw,19px);color:rgba(255,255,255,0.58);font-weight:300;letter-spacing:0.08em;margin-bottom:40px;opacity:0;transform:translateY(16px);">촬영보다 본식이 더 예뻤어요</p>
        <div id="hero-cta" style="display:flex;gap:14px;flex-wrap:wrap;opacity:0;transform:translateY(16px);pointer-events:auto;">
          <a href={`${base}/contact`} class="btn-primary magnetic">피팅 예약하기</a>
          <a href={`${base}/gallery`} class="btn-outline magnetic">드레스 보러가기</a>
        </div>
      </div>
    </div>
    <!-- 진행 표시 -->
    <div style="position:absolute;bottom:40px;right:8vw;z-index:3;text-align:right;">
      <div style="width:60px;height:1px;background:rgba(255,255,255,0.15);margin-bottom:8px;">
        <div id="vp1" style="height:100%;width:0%;background:linear-gradient(90deg,var(--gold),var(--gold-light));transition:width 0.1s;"></div>
      </div>
      <p style="font-family:'Cormorant Garamond',serif;font-size:11px;color:rgba(255,255,255,0.25);letter-spacing:0.2em;">01 · 03</p>
    </div>
    <div id="scroll-arrow" style="position:absolute;bottom:40px;left:50%;transform:translateX(-50%);z-index:3;display:flex;flex-direction:column;align-items:center;gap:8px;">
      <span style="font-size:11px;letter-spacing:0.2em;color:rgba(255,255,255,0.45);">아래로 내려보세요</span>
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="rgba(212,175,117,0.7)" stroke-width="1.5" style="animation:bounce 1.8s ease-in-out infinite;"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
    </div>
  </div>
</section>

<!-- ══════ VIDEO SCROLL — 섹션 2 (frames 065~128) ════════════════════════ -->
<section id="vs2" style="height:350vh;position:relative;">
  <div style="position:sticky;top:0;height:100dvh;overflow:hidden;background:#070305;">
    <canvas id="vc2" style="position:absolute;inset:0;width:100%;height:100%;z-index:1;transition:opacity 0.6s ease;"></canvas>
    <div style="position:absolute;inset:0;z-index:2;background:linear-gradient(to bottom,rgba(7,3,5,0.3) 0%,rgba(7,3,5,0.05) 50%,rgba(7,3,5,0.75) 100%);pointer-events:none;"></div>
    <div style="position:absolute;inset:0;z-index:2;background:radial-gradient(ellipse at center,transparent 50%,rgba(7,3,5,0.4) 100%);pointer-events:none;"></div>
    <div style="position:absolute;inset:0;z-index:3;display:flex;flex-direction:column;justify-content:flex-end;padding:0 8vw 12vh;pointer-events:none;">
      <div style="max-width:750px;">
        <p style="font-size:10px;letter-spacing:0.5em;text-transform:uppercase;color:rgba(212,175,117,0.7);margin-bottom:16px;">Our Collection</p>
        <h2 class="font-display" style="font-size:clamp(52px,9vw,108px);font-weight:300;color:white;line-height:0.88;letter-spacing:0.02em;margin-bottom:20px;">120 Dresses</h2>
        <p style="font-size:clamp(14px,1.6vw,17px);color:rgba(255,255,255,0.55);font-weight:300;max-width:480px;line-height:1.75;">국내외 최신 트렌드 웨딩드레스 120벌 보유.<br/>에이라인, 볼가운, 슬림핏, 레이스까지 모든 스타일.</p>
      </div>
    </div>
    <div style="position:absolute;bottom:40px;right:8vw;z-index:3;text-align:right;">
      <div style="width:60px;height:1px;background:rgba(255,255,255,0.15);margin-bottom:8px;">
        <div id="vp2" style="height:100%;width:0%;background:linear-gradient(90deg,var(--gold),var(--gold-light));transition:width 0.1s;"></div>
      </div>
      <p style="font-family:'Cormorant Garamond',serif;font-size:11px;color:rgba(255,255,255,0.25);letter-spacing:0.2em;">02 · 03</p>
    </div>
  </div>
</section>

<!-- ══════ VIDEO SCROLL — 섹션 3 (frames 129~192) ════════════════════════ -->
<section id="vs3" style="height:350vh;position:relative;">
  <div style="position:sticky;top:0;height:100dvh;overflow:hidden;background:#070305;">
    <canvas id="vc3" style="position:absolute;inset:0;width:100%;height:100%;z-index:1;transition:opacity 0.6s ease;"></canvas>
    <div style="position:absolute;inset:0;z-index:2;background:linear-gradient(to bottom,rgba(7,3,5,0.3) 0%,rgba(7,3,5,0.05) 50%,rgba(7,3,5,0.75) 100%);pointer-events:none;"></div>
    <div style="position:absolute;inset:0;z-index:2;background:radial-gradient(ellipse at center,transparent 50%,rgba(7,3,5,0.4) 100%);pointer-events:none;"></div>
    <div style="position:absolute;inset:0;z-index:3;display:flex;flex-direction:column;justify-content:flex-end;padding:0 8vw 12vh;pointer-events:none;">
      <div style="max-width:750px;">
        <p style="font-size:10px;letter-spacing:0.5em;text-transform:uppercase;color:rgba(212,175,117,0.7);margin-bottom:16px;">Signature System</p>
        <h2 class="font-display" style="font-size:clamp(52px,9vw,108px);font-weight:300;color:white;line-height:0.88;letter-spacing:0.02em;margin-bottom:20px;">Two Fittings</h2>
        <p style="font-size:clamp(14px,1.6vw,17px);color:rgba(255,255,255,0.55);font-weight:300;max-width:480px;line-height:1.75;">3개월 전 1차 피팅(3벌) + 1개월 전 2차 피팅(3벌).<br/>타샵과 차별화된 전문 피팅 시스템.</p>
      </div>
    </div>
    <div style="position:absolute;bottom:40px;right:8vw;z-index:3;text-align:right;">
      <div style="width:60px;height:1px;background:rgba(255,255,255,0.15);margin-bottom:8px;">
        <div id="vp3" style="height:100%;width:0%;background:linear-gradient(90deg,var(--gold),var(--gold-light));transition:width 0.1s;"></div>
      </div>
      <p style="font-family:'Cormorant Garamond',serif;font-size:11px;color:rgba(255,255,255,0.25);letter-spacing:0.2em;">03 · 03</p>
    </div>
  </div>
</section>

<!-- ══════ STATS 섹션 ══════════════════════════════════════════════════════ -->
<section id="stats-section" style="background:var(--dark);position:relative;z-index:2;overflow:hidden;">
  <!-- 배경 글로우 -->
  <div style="position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);width:600px;height:300px;background:radial-gradient(ellipse,rgba(212,175,117,0.06) 0%,transparent 70%);pointer-events:none;"></div>
  <div style="max-width:900px;margin:0 auto;display:grid;grid-template-columns:repeat(4,1fr);position:relative;">
    {stats.map((s, idx) => (
      <div class="stat-item">
        <span class="stat-num counter-num" data-target={s.num} data-suffix={s.suffix}>0{s.suffix}</span>
        <span class="stat-label">{s.label}</span>
      </div>
    ))}
  </div>
  <!-- 상하 구분선 -->
  <div style="position:absolute;top:0;left:0;right:0;height:1px;background:linear-gradient(90deg,transparent,rgba(212,175,117,0.15),transparent);"></div>
  <div style="position:absolute;bottom:0;left:0;right:0;height:1px;background:linear-gradient(90deg,transparent,rgba(212,175,117,0.15),transparent);"></div>
</section>

<!-- ══════ MARQUEE ══════════════════════════════════════════════════════════ -->
<div class='marquee-outer' style='position:relative;z-index:2;'>
  <div class='marquee-track'>
    {[1,2].map(() => (
      ['WEDDING MARCH','춘천 웨딩드레스','SINCE 2017','드레스 120벌','2회 피팅 시스템','원스톱 웨딩','CHUNCHEON BRIDAL','미래웨딩홀 입점'].map(t => (
        <span class='marquee-item'>{t} ·</span>
      ))
    ))}
  </div>
</div>

<!-- ══════ 갤러리 섹션 ══════════════════════════════════════════════════════ -->
<section style="background:var(--dark);padding:80px 0 60px;position:relative;z-index:2;">
  <div style="max-width:1100px;margin:0 auto;padding:0 8vw;margin-bottom:40px;display:flex;justify-content:space-between;align-items:flex-end;flex-wrap:wrap;gap:16px;">
    <div>
      <span class='section-label gsap-fade' style="color:rgba(212,175,117,0.7);">Our Collection</span>
      <h2 class='font-display gsap-fade' style="font-size:clamp(32px,5vw,56px);font-weight:300;color:white;letter-spacing:0.04em;">드레스 갤러리</h2>
    </div>
    <a href={`${base}/gallery`} class="gsap-fade" style="font-size:10px;letter-spacing:0.3em;text-transform:uppercase;color:var(--gold);text-decoration:none;border-bottom:1px solid rgba(212,175,117,0.35);padding-bottom:3px;transition:all 0.3s;white-space:nowrap;"
       onmouseover="this.style.borderBottomColor='#D4AF75'" onmouseout="this.style.borderBottomColor='rgba(212,175,117,0.35)'">
      전체 갤러리 →
    </a>
  </div>
  <div id="hgal-track" class="hgal-track" style="padding:0 8vw;">
    {galleryItems.map((item) => (
      <div class="hgal-item">
        <img src={item.src} alt={`웨딩마치 ${item.label}`} loading='lazy' />
        <div class="hgal-label">{item.label}</div>
      </div>
    ))}
  </div>
</section>

<!-- ══════ 리뷰 ══════════════════════════════════════════════════════════════ -->
<section style="background:var(--dark);padding:80px 8vw 100px;position:relative;z-index:2;border-top:1px solid rgba(255,255,255,0.05);">
  <div style="max-width:1100px;margin:0 auto;">
    <div style="text-align:center;margin-bottom:60px;">
      <span class='section-label gsap-fade' style="color:rgba(212,175,117,0.7);">Bride Reviews</span>
      <h2 class='font-display gsap-fade' style="font-size:clamp(30px,4vw,52px);font-weight:300;color:white;letter-spacing:0.04em;">신부들의 이야기</h2>
    </div>
    <!-- Supanova: 비대칭 리뷰 레이아웃 — 첫 카드 featured (2배 너비) -->
    <div style="display:grid;grid-template-columns:2fr 1fr 1fr;gap:1px;">
      {reviews.map((rv, idx) => (
        <div class="review-card" data-delay={idx * 0.15}
          style={idx === 0 ? "border-left:3px solid var(--gold);padding:44px 44px 36px;" : ""}>
          <div style="font-family:'Cormorant Garamond',serif;font-size:60px;line-height:1;color:rgba(212,175,117,0.1);margin-bottom:14px;font-weight:300;">"</div>
          <div style="display:flex;gap:2px;margin-bottom:18px;">
            {'★★★★★'.split('').map(s => <span style="color:var(--gold);font-size:12px;">{s}</span>)}
          </div>
          <p style={`line-height:1.85;color:rgba(255,255,255,0.55);font-weight:300;font-style:italic;margin-bottom:24px;font-size:${idx===0?'16px':'14px'};`}>"{rv.text}"</p>
          <div style="display:flex;align-items:center;gap:12px;border-top:1px solid rgba(255,255,255,0.06);padding-top:18px;">
            <div style="width:40px;height:40px;border-radius:50%;overflow:hidden;flex-shrink:0;border:1px solid rgba(212,175,117,0.25);">
              <img src={rv.img} alt="신부" style="width:100%;height:100%;object-fit:cover;" loading='lazy' />
            </div>
            <div>
              <p style="font-size:11px;color:var(--gold);letter-spacing:0.15em;">{rv.name}</p>
              <p style="font-size:10px;color:rgba(255,255,255,0.28);margin-top:2px;">{rv.date}</p>
            </div>
          </div>
        </div>
      ))}
    </div>
    <!-- 모바일: 1열 -->
    <style>@media(max-width:768px){.reviews-grid{grid-template-columns:1fr!important;}}</style>
  </div>
</section>

<!-- ══════ 인스타 스트립 ══════════════════════════════════════════════════════ -->
<section style="background:var(--dark);padding-bottom:0;overflow:hidden;position:relative;z-index:2;">
  <!-- 헤더 -->
  <div style="padding:60px 8vw 32px;max-width:1100px;margin:0 auto;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:16px;">
    <div>
      <span class='section-label gsap-fade' style="color:rgba(212,175,117,0.7);">Instagram</span>
      <h3 class='font-display gsap-fade' style="font-size:clamp(26px,4vw,42px);font-weight:300;color:white;">@ch_weddingmarch</h3>
    </div>
    <a href="https://www.instagram.com/ch_weddingmarch/" target="_blank" rel="noopener"
       class="gsap-fade btn-gold" style="white-space:nowrap;">팔로우</a>
  </div>
  <!-- 6 photos 그리드 -->
  <div style="display:grid;grid-template-columns:repeat(6,1fr);gap:2px;">
    {['155','165','270','465','1450','1465'].map(n => (
      <a href='https://www.instagram.com/ch_weddingmarch/' target='_blank' rel='noopener'
         class="insta-tile"
         style="display:block;aspect-ratio:1;overflow:hidden;position:relative;">
        <img src={`${base}/images/instagram/${n}.webp`} alt='웨딩마치 인스타그램'
             style="width:100%;height:100%;object-fit:cover;transition:transform 0.6s cubic-bezier(0.23,1,0.32,1);"
             loading='lazy' />
        <div class="insta-hover" style="position:absolute;inset:0;background:rgba(7,3,5,0.55);display:flex;align-items:center;justify-content:center;opacity:0;transition:opacity 0.4s;">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="1.5">
            <rect x="2" y="2" width="20" height="20" rx="5" ry="5"/>
            <circle cx="12" cy="12" r="4"/>
            <circle cx="17.5" cy="6.5" r="1" fill="white" stroke="none"/>
          </svg>
        </div>
      </a>
    ))}
  </div>
</section>

<!-- ══════ 최종 CTA ══════════════════════════════════════════════════════════ -->
<section style="background:var(--dark);padding:130px 8vw 120px;text-align:center;position:relative;z-index:2;overflow:hidden;border-top:1px solid rgba(255,255,255,0.06);">
  <!-- 배경 오브 -->
  <div style="position:absolute;top:-80px;left:-80px;width:500px;height:500px;border-radius:50%;background:radial-gradient(circle,rgba(183,110,121,0.12),transparent 70%);pointer-events:none;"></div>
  <div style="position:absolute;bottom:-100px;right:-60px;width:600px;height:600px;border-radius:50%;background:radial-gradient(circle,rgba(212,175,117,0.08),transparent 70%);pointer-events:none;"></div>
  <div style="max-width:640px;margin:0 auto;position:relative;">
    <span class='section-label gsap-fade'>Start Your Journey</span>
    <h2 class='font-display' style="font-size:clamp(38px,6.5vw,76px);font-weight:300;color:white;line-height:1.02;margin-bottom:20px;letter-spacing:0.02em;">
      당신의 특별한 날,<br/>
      <span class='gold-gradient' style="font-style:italic;">웨딩마치와 함께</span>
    </h2>
    <div class='section-divider'></div>
    <p class='gsap-fade' style="font-size:16px;color:rgba(255,255,255,0.5);font-weight:300;line-height:1.85;margin-bottom:44px;">
      춘천 최고의 웨딩드레스 전문샵에서<br/>평생 기억될 드레스를 만나보세요.
    </p>
    <a href={`${base}/contact`} class='btn-primary magnetic' style="padding:18px 60px;font-size:12px;">무료 상담 예약하기</a>
  </div>
</section>

</Layout>

<style>
/* 타이틀 슬라이드업 — CSS만으로 동작, JS 불필요 */
.title-slide-up {
  transform: translateY(110%);
  animation: titleSlideUp 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}
@keyframes titleSlideUp {
  to { transform: translateY(0); }
}
/* h1 자체는 처음부터 보임 (overflow:hidden wrapper가 텍스트 가림) */
.hero-title-anim { opacity: 1; }

@keyframes lineDown {
  0%, 100% { opacity: 1; transform: scaleY(1); transform-origin: top; }
  50% { opacity: 0.3; transform: scaleY(0.5); transform-origin: top; }
}
@keyframes bounce {
  0%, 100% { transform: translateX(-50%) translateY(0); }
  50% { transform: translateX(-50%) translateY(8px); }
}
/* 인스타 타일 호버 */
.insta-tile:hover img { transform: scale(1.08); }
.insta-tile:hover .insta-hover { opacity: 1; }

/* 모바일 stats grid */
@media (max-width: 600px) {
  #stats-section > div > div:not(:first-child)::before { display: none; }
}
</style>

<script>
  const gsap = (window as any).gsap;
  const ScrollTrigger = (window as any).ScrollTrigger;
  const SplitType = (window as any).SplitType;

  // ── 비디오 스크롤 플레이어 팩토리 ──────────────────────────────────────────
  (function initVideoScrollPlayers() {
    const base = (document.querySelector('meta[name="site-base"]') as HTMLMetaElement)?.content
      || (window.location.pathname.includes('/weddingmarch-site') ? '/weddingmarch-site' : '');

    // [섹션ID, 캔버스ID, 진행바ID, 시작프레임(1-based), 끝프레임(1-based)]
    const sections: [string, string, string, number, number][] = [
      ['vs1', 'vc1', 'vp1', 1,   64],
      ['vs2', 'vc2', 'vp2', 65,  128],
      ['vs3', 'vc3', 'vp3', 129, 192],
    ];

    sections.forEach(([secId, canvasId, progressId, startFrame, endFrame]) => {
      const section = document.getElementById(secId) as HTMLElement | null;
      const canvas  = document.getElementById(canvasId) as HTMLCanvasElement | null;
      const progEl  = document.getElementById(progressId) as HTMLElement | null;
      if (!section || !canvas) return;

      const ctx = canvas.getContext('2d');
      if (!ctx) return;

      const FRAMES = endFrame - startFrame + 1;

      function resize() {
        canvas.width  = window.innerWidth;
        canvas.height = window.innerHeight;
      }
      resize();
      window.addEventListener('resize', resize, { passive: true });

      // 프레임 프리로드
      const imgs: HTMLImageElement[] = [];
      for (let i = 0; i < FRAMES; i++) {
        const img = new Image();
        img.src = `${base}/images/hero/frame_${String(startFrame + i).padStart(3,'0')}.webp`;
        if (i === 0) img.onload = () => draw(0);
        imgs.push(img);
      }

      function draw(idx: number) {
        const img = imgs[idx];
        if (!img?.complete || !img.naturalWidth) return;
        const cw = canvas.width, ch = canvas.height;
        const sc = Math.max(cw / img.naturalWidth, ch / img.naturalHeight);
        const dw = img.naturalWidth * sc, dh = img.naturalHeight * sc;
        ctx.clearRect(0, 0, cw, ch);
        ctx.drawImage(img, (cw-dw)/2, (ch-dh)/2, dw, dh);
      }

      let target = 0, display = 0, raf: number | null = null;

      function onScroll() {
        const top    = section.getBoundingClientRect().top + window.scrollY;
        const height = section.offsetHeight - window.innerHeight;
        const prog   = Math.min(Math.max((window.scrollY - top) / height, 0), 1);
        target = Math.round(prog * (FRAMES - 1));
        if (progEl) progEl.style.width = `${prog * 100}%`;

        // 섹션 끝 20% 구간에서 페이드아웃
        const fadeZone = 0.2;
        const opacity = prog > 1 - fadeZone
          ? Math.max(0, (1 - prog) / fadeZone)
          : 1;
        canvas.style.opacity = String(opacity);

        // 섹션1 스크롤 화살표 페이드
        if (secId === 'vs1') {
          const arrow = document.getElementById('scroll-arrow');
          if (arrow) arrow.style.opacity = String(Math.max(0, 1 - prog * 8));
        }
        if (raf === null) raf = requestAnimationFrame(loop);
      }

      function loop() {
        raf = null;
        const diff = target - display;
        if (Math.abs(diff) > 0.5) {
          display += diff * 0.25;
          draw(Math.round(display));
          raf = requestAnimationFrame(loop);
        } else {
          display = target;
          draw(target);
        }
      }

      window.addEventListener('scroll', onScroll, { passive: true });
      draw(0);
    });
  })();

  // ── GSAP 초기화 ─────────────────────────────────────────────────────────
  if (gsap && ScrollTrigger) {
    gsap.registerPlugin(ScrollTrigger);

    // ── 히어로 진입 애니메이션 ──────────────────────────────────────────
    const eyebrowLine = document.getElementById('eyebrow-line') as HTMLElement | null;
    const eyebrowText = document.getElementById('eyebrow-text') as HTMLElement | null;
    const heroSub = document.getElementById('hero-sub') as HTMLElement | null;
    const heroCta = document.getElementById('hero-cta') as HTMLElement | null;

    // 아이브로우 라인
    if (eyebrowLine) {
      setTimeout(() => {
        eyebrowLine.style.width = '60px';
      }, 300);
    }
    if (eyebrowText) {
      setTimeout(() => {
        eyebrowText.style.opacity = '1';
      }, 900);
    }

    // 서브타이틀 + CTA
    if (heroSub) {
      gsap.to(heroSub, { opacity: 1, y: 0, duration: 0.9, ease: 'power3.out', delay: 1.4 });
    }
    if (heroCta) {
      gsap.to(heroCta, { opacity: 1, y: 0, duration: 0.9, ease: 'power3.out', delay: 1.65 });
    }

    // ── 일반 gsap-fade ────────────────────────────────────────────────────
    document.querySelectorAll('.gsap-fade').forEach(el => {
      gsap.from(el, {
        y: 35, opacity: 0, duration: 0.85, ease: 'power3.out',
        scrollTrigger: { trigger: el, start: 'top 82%' },
      });
    });

    // ── 리뷰 카드 reveal ──────────────────────────────────────────────────
    document.querySelectorAll<HTMLElement>('.review-card').forEach((card) => {
      const delay = parseFloat(card.dataset.delay || '0');
      gsap.to(card, {
        opacity: 1, y: 0, duration: 0.9, ease: 'power3.out',
        scrollTrigger: { trigger: card, start: 'top 82%' },
        delay,
      });
    });

    // ── 카운터 애니메이션 ─────────────────────────────────────────────────
    document.querySelectorAll<HTMLElement>('.counter-num').forEach(el => {
      const target = parseInt(el.dataset.target || '0');
      const suffix = el.dataset.suffix || '';
      ScrollTrigger.create({
        trigger: el,
        start: 'top 85%',
        once: true,
        onEnter: () => {
          const obj = { val: 0 };
          gsap.to(obj, {
            val: target,
            duration: 2.0,
            ease: 'power2.out',
            onUpdate: () => {
              el.textContent = Math.round(obj.val) + suffix;
            },
          });
        },
      });
    });

    // ── 인스타 그리드 stagger reveal ──────────────────────────────────────
    gsap.from('.insta-tile', {
      opacity: 0, scale: 0.92, duration: 0.6, stagger: 0.08, ease: 'power2.out',
      scrollTrigger: { trigger: '.insta-tile', start: 'top 85%' },
    });

  } else {
    // GSAP 없을 때 fallback
    document.querySelectorAll<HTMLElement>('.gsap-fade').forEach(el => { el.style.opacity = '1'; el.style.transform = 'none'; });
    document.querySelectorAll<HTMLElement>('.review-card').forEach(el => { el.style.opacity = '1'; el.style.transform = 'none'; });
    document.querySelectorAll<HTMLElement>('#hero-sub,#hero-cta').forEach(el => { el.style.opacity = '1'; el.style.transform = 'none'; });
    document.querySelectorAll<HTMLElement>('#eyebrow-text').forEach(el => { el.style.opacity = '1'; });
  }

  // ── 마그네틱 버튼 ─────────────────────────────────────────────────────
  document.querySelectorAll<HTMLElement>('.magnetic').forEach(btn => {
    btn.addEventListener('mousemove', (e: MouseEvent) => {
      const rect = btn.getBoundingClientRect();
      const x = (e.clientX - rect.left - rect.width / 2) * 0.18;
      const y = (e.clientY - rect.top - rect.height / 2) * 0.18;
      btn.style.transform = `translate(${x}px,${y}px)`;
    });
    btn.addEventListener('mouseleave', () => {
      btn.style.transform = '';
    });
  });

  // ── 가로 갤러리 드래그 ────────────────────────────────────────────────
  const track = document.getElementById('hgal-track') as HTMLElement | null;
  if (track) {
    let isDown = false, startX = 0, scrollLeft = 0;
    track.addEventListener('mousedown', e => {
      isDown = true; track.style.cursor = 'grabbing';
      startX = e.pageX - track.offsetLeft;
      scrollLeft = track.scrollLeft;
    });
    ['mouseleave', 'mouseup'].forEach(ev => track.addEventListener(ev, () => { isDown = false; track.style.cursor = 'grab'; }));
    track.addEventListener('mousemove', e => {
      if (!isDown) return;
      e.preventDefault();
      track.scrollLeft = scrollLeft - (e.pageX - track.offsetLeft - startX) * 1.5;
    });
    track.style.cursor = 'grab';
    let tx = 0;
    track.addEventListener('touchstart', e => { tx = e.touches[0].clientX; }, { passive: true });
    track.addEventListener('touchmove', e => {
      track.scrollLeft += tx - e.touches[0].clientX;
      tx = e.touches[0].clientX;
    }, { passive: true });
  }
</script>
